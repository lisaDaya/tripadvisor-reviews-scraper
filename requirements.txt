beautifulsoup4==4.10.0
requests==2.26.0

You can now run this project on your local machine! Just make sure to install the dependencies using pip install -r requirements.txt and run the scraper.py file to start scraping reviews.